/* Custom API exports - NOT generated, safe to edit */

// Export the pre-configured API client (recommended)
export { apiClient, createApiClient, getApiBaseUrl } from './config';

// Re-export everything from the generated API
export * from './index';

// You can add custom API utilities here that won't be overwritten
// during API regeneration
